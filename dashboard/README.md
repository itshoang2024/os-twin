# OS Twin Dashboard

API server, knowledge graph, and management UI for OS Twin.

This directory is also a Python package (`ostwin-dashboard`). The package
metadata in `pyproject.toml` points to this README, so it must be tracked with
the dashboard source for editable installs and CI smoke tests.
