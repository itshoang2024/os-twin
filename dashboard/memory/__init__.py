"""Memory MCP server and CLI tools.

The core memory library lives in ``dashboard.agentic_memory``.
This package contains the MCP server (stdio + SSE transports),
the CLI tool, and supporting shell scripts.
"""
